<!DOCTYPE html>
<html lang="en">
  <body>

<div class="container">
<div class="jumbotron">
<%- model_class = Faq -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:title) %>:</strong></dt>
  <dd><%= @faq.title %></dd>
  <dt><strong><%= model_class.human_attribute_name(:body) %>:</strong></dt>
  <dd><%= @faq.body %></dd>
</dl>


<p id="notice"><%= notice %></p>

<div class="form-actions">
	<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_faq_path(@faq), :class => 'btn' %> |
	<%= link_to t('.back', :default => t("helpers.links.back")), faqs_path, :class => 'btn'%>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              faq_path(@faq),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
</div>
<hr>
        <footer>
          <a href="https://www.facebook.com/pages/The-Oval-Ski-Club/240654432678815?fref=ts"><img src="../assets/icon-facebook.png"/></a>
          <p>&copy; Company 2012</p>
        </footer>
      </hr>
  </div>
   
    </div>
</body>
</html>